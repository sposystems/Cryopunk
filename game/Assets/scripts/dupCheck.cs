using UnityEngine;
using System.Collections;

public class dupCheck : MonoBehaviour {

	// Use this for initialization
	void Start () {
		if(GameObject.Find("Swordman 1") != null){
			//It would be better if on start we load a scene which inits the character and stuff.
		}
	}
	
	// Update is called once per frame

}
